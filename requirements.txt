streamlit>=1.37.0  # for st.fragment
openai>=1.35.0
anthropic>=0.40.0
jsonschema>=4.23.0
PyMuPDF>=1.24.0

# Apple Vision on-device OCR (macOS only). Enables the "Apple Vision OCR" PDF
# modes. The env marker makes pip skip it on non-macOS. Pulls in Pillow + pyobjc.
ocrmac ; sys_platform == "darwin"

# On-device Surya OCR (datalab-to/surya, Apache-2.0) — the recommended OCR, run
# IN-PROCESS in this same env. A 650M model served by llama.cpp/Metal, ~3 GB, loaded
# once and kept warm. Because Surya pins openai<2 + opencv wheels, run the WHOLE app
# on Python 3.10-3.13 (NOT 3.14 — no opencv wheel there). One-time setup:
#   brew install llama.cpp
#   pip install surya-ocr          # into this same venv
# The "Surya OCR" PDF modes appear automatically once surya + llama-server exist.
